/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
        // serverUrl: "https://backend-inventory-git-release-mfitrie78-gmailcom.vercel.app",
        // serverUrl: "http://localhost:4000",
        serverUrl: "https://backend-inventory-delta.vercel.app",
    }
};

export default nextConfig;
